"""AuData — biomedical research-integrity auditor backend.

A standalone FastAPI service, separate from the legacy Evidence Engine app
(api.py). It depends only on its own modules + env, not on EE's endpoints.
"""
