Put a PDF at `samples/test_paper.pdf` to test the auditor:

```bash
python -m auditor ./samples/test_paper.pdf
```

The repository also includes a tiny generated sample PDF at that path. Replace
it with any real paper PDF when you are ready.
